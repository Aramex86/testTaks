const baseUrl = "http://localhost:3000";

export const siteApi = {
  getSite: `${baseUrl}/sites`,
  getSiteById: `${baseUrl}/sites`,
};
export const testApi = {
  getTest: `${baseUrl}/tests`,
  getTestById: `${baseUrl}/tests`,
};
